@model IEnumerable<SharedModels.Models.CamposPesquisa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tags.Tag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdCampo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoCampo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tags.Tag)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdCampo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoCampo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CamposPesquisaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CamposPesquisaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CamposPesquisaId })
        </td>
    </tr>
}

</table>
